"""Hybrid memory layer: relational (Supabase/Postgres) + vector (Chroma).

Import the concrete classes from their submodules to avoid pulling heavy optional
dependencies at package import time::

    from memory.relational import RelationalMemory
    from memory.vector import VectorMemory
"""
