"""The Tsumiki Engine — deterministic, non-LLM world-state updates.

Populated in a later task (apply_event / stone-variant logic). Kept separate from
the agent graph so visual outcomes are reliable and unit-testable.
"""
